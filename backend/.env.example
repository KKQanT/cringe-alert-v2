# Google AI
GOOGLE_API_KEY=your-google-api-key

# Firebase
FIREBASE_BUCKET=your-firebase-bucket
FIREBASE_CREDENTIALS_PATH=credentials/your-credentials.json

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Auth (format: user1:pass1,user2:pass2)
AUTH_USERS=judge:your-password,admin:your-password
JWT_SECRET=your-jwt-secret

# Server
HOST=0.0.0.0
PORT=8000
